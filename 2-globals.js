// VARIAVEIS GLOBAIS
// __dirname = caminho pro diretorio atual
// __filename = arquivo
// requie = funcao pra usar modules(CommonJS)
// module = info do module atual (file)
// process = info do ambiente que o prog ta sendo exec

console.log(__dirname);

setInterval(()=>{
    console.log('hello world');
}, 1000)